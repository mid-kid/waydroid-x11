#!/bin/sh
set -eu

trap 'exit $?' INT QUIT TERM
trap 'waydroid session stop' EXIT
weston --backend=x11 --width=450 --height=800 -c '@pkgdatadir@/weston.ini' "$@" -- /bin/sh -c 'sleep 2 && exec waydroid show-full-ui'
